# \${pkg.name}

${badge('npm')} ${badge('npm-license')} \${badge('github-issues')}

\${pkg.description}

\${badge('nodei')}

${badge('travis-status')}
${badge('github-stars')}
\${badge('github-forks')}

\${badge('github-forks')}

${badge('dependencies')}
${badge('devDependencies')}

## Features

- Configure express application.
- Written in TS
